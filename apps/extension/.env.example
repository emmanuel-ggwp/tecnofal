# Proveedor de backend (§21): nhost (activo hoy) | supabase (futuro)
VITE_PROVIDER=nhost

# --- Nhost --- (dashboard del proyecto → subdomain y region)
VITE_NHOST_SUBDOMAIN=
VITE_NHOST_REGION=us-east-1

# --- Supabase --- (solo si VITE_PROVIDER=supabase)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
